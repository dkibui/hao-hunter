<div class="container">
  <h1>All listings</h1>
  <% @listings.each do |listing| %>
  <div class="card">
    <h2><%= listing.title %></h2>
    <p>Bedrooms: <%= listing.bedrooms %></p>
  </div>
  <% end %>
</div>