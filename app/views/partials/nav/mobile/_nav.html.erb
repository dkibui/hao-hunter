<nav class="mobile">
  <div class="nav-menu">
    <?xml version="1.0" ?>
    <svg fill="none" viewBox="4 0 16 24" class="hamburger-menu"
         xmlns="http://www.w3.org/2000/svg"><path d="M4 6H20M4 12H20M4 18H20" stroke="" stroke-linecap="round" stroke-linejoin="round" stroke-width="1"/></svg>
    <div class="logo">
      <%= link_to raw(render "partials/logo"), root_path %>
    </div>
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Login", new_user_session_path %>
    <% end %>
  </div>
</nav>
